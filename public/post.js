var template = "<li></li>";

(function() {
	var postElem = document.getElementById("post");
	
	// Get request for most recent post.
	
	
})();

class Page {
	constructor(selectedPost) {
		this.selectedPost = selectedPost;
		this.render();
	}
	render() {
		let postsElem = document.getElementById("id");
		
		thi
	}
}

class Link {
	
}

class Post {
	constructor(title, content, author) {
		this.title = title;
		this.content = content;
		this.author = author;
	}
	render() {
		
	}
	change(event) {
		this.updateDOM(event);
	}
	updateDOM(properties) {
		
	}
}